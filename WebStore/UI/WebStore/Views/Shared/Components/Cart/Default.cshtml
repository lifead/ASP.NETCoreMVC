@model CartViewModel

@{ 
    var product_count = Model.ItemsCount > 0 ? Model.ItemsCount : (int?)null;
}
    <div>
        <a asp-controller="Cart" asp-action="Details">
            <i class="fa fa-shopping-cart"></i>&nbsp;Корзина&nbsp;@product_count
        </a>
    </div>